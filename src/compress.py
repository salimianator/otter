"""
compress.py — OTTER context compressor (stub)

The compress() function will take a long context and a query and return
a shorter context that preserves the information needed to answer the
query. Implementation TBD.
"""


def compress(context: str, query: str) -> None:
    """Compress context given a query. Implementation TBD."""
    return None
